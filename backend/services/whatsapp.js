// WhatsApp service (Disabled)
// Automatic WhatsApp messages are disabled as requested.

async function sendWhatsAppMessage(phone, message) {
  // WhatsApp messages disabled
  return false;
}

module.exports = { sendWhatsAppMessage };
