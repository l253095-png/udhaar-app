import 'package:flutter/material.dart';

/// "Bazaar Signboard" palette — inspired by Pakistani truck-art and
/// hand-painted market signboards.
class AppColors {
  static const deepIndigo = Color(0xFF1B3A4B);   // signboard base, headers
  static const marigold = Color(0xFFF4A322);     // primary accent
  static const rickshawRed = Color(0xFFD62828);  // udhaar / debit
  static const truckGreen = Color(0xFF2A9D5C);   // wasooli / credit
  static const warmCream = Color(0xFFFFF8ED);    // background / paper feel
  static const gulabiPink = Color(0xFFE85D75);   // rare highlight / badges
}